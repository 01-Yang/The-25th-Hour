# 工作日志 2026-05-18

## 本次修改范围

- 只按本次需求修改事件、数值、系统规则、内容池简介、成就和 ADR 口径，没有改动既有结局正文、既有成就标题正文或已确认事件正文。

## 事件系统

- 在 `events.md` 新增固定流程事件：`开学典礼 / 军训 / 建院生活开始提示 / 毕业典礼`。
- 明确开学典礼与军训发生在正式周行动前，期间行动、商店、竞赛、实习、路线报名不可使用。
- 新增普通随机事件：`天选之人 / 睡爽了 / 义父的拯救 / 轻松绷住 / 陶吉吉 / 周杰伦 / 五月天 / 转专业 / 天上馅饼`。
- 新增交互事件：`暧昧 / 电影 / 演唱会`。
- 新增触发限定：`轻松绷住` 大一下必定触发；AI 相关事件从大一下开放，并在大一下至大三下总共保底触发 `4` 次；有女朋友后暧昧与搭讪事件不再触发。
- 将点名解围事件标题确定为 `义父的拯救`，避免与原有送饭事件 `义父` 重名。

## 数值系统

- 在 `numbers.md` 补齐开学典礼、军训、毕业典礼的固定流程结算。
- 补齐新增普通随机事件和交互事件的属性结算。
- 新增暧昧系统门槛：人际交往 >= `85`、精力 >= `45`、压力 <= `55`、创意审美 >= `70` 时可触发 `两情相悦`。
- 明确 `已有女朋友 = true` 后，暧昧与搭讪事件权重归零，`恋爱危机` 只在已有女朋友后进入事件池。
- 补充 `接设计外包` 能力门槛和一周内不限次数规则；补充 `校外兼职` 低社交门槛和一周内不限次数规则。

## 商店、竞赛、成就

- 在商店清单新增 `音乐会员`。
- 取消推荐商店口径，商店只展示完整商品清单、分类和可购买状态。
- 给所有商店商品补充一句简介。
- 将竞赛规则改为每学期最多投稿 `1` 次，并同步到 `systems.md / numbers.md / development-freeze.md / adr/0002`。
- 给四项竞赛补充一句简介，并保留原有门槛、公式、奖金结算口径。
- 新增成长成就：`外星人拥有者 / 两情相悦`。

## 内容池

- 在 `content-plan.md` 新增角色简介表，覆盖当前首发角色池。
- 在 `content-plan.md` 新增导师简介表，覆盖当前导师池。

## 事件频率与阶段权重

- 将单局主事件目标从旧口径 `84` 次调整为 `90-95` 次，标准目标按 `92` 次校准。
- 新增按学年统计：大一 `22`、大二 `22`、大三 `18`、大四 `16`、大五 `14`。
- 新增按事件池统计：普通随机事件 `56`、交互事件 `28`、模型周事件 `4`、万里路事件 `4`。
- 明确固定流程事件和暑假写生节点不计入主事件目标。
- 明确交互事件触发后默认追加 `1` 次普通随机事件，连续 `2` 次普通随机事件概率目标为 `15%`。
- 上调大一、大二事件触发概率；交互事件权重同时照顾学业、社交/朋友/暧昧、娱乐/恢复，推荐结构为 `40% / 35% / 25%`。
- 将 AI 相关事件开放时间从大二上调整为大一下，仍不新增独立 AI 事件池。
- 新增专题 PRD：`PRD-event-frequency-and-stage-weighting.md`。

## 本轮追加修改

- 在 `content-plan.md` 更新导师标签名：王老师为 `景观公园实践大师`，戈老师为 `高级学院派`，林老师为 `高压审美者`，陈老师为 `理想主义者`，周老师为 `软件技术大神`，许老师为 `佛系放养家`，韩老师为 `竞赛压力怪`。
- 在 `content-plan.md` 写入并对齐导师简介、角色简介、保研/考研简介、考公/考编/选调岗位简介、建筑相关工作池简介、转行岗位池简介。
- 在 `numbers.md` 同步保研与考研路线表的简介列，并保留原申请门槛、考试分数线和结局读取不变。
- 在 `numbers.md` 给建筑工作路线组、考公、考编、选调、转行路线组补充 `简介` 列；只补文案，不改路线门槛和结局读取。
- 在 `numbers.md` 明确实习工作池读取建筑相关工作池，不包括转行岗位池。
- 在 `numbers.md` 同步竞赛简介和商店商品简介，新增并保留 `音乐会员` 商品数值结算。
- 在 `events.md` 补充 `陶吉吉 / 周杰伦 / 五月天` 三个音乐事件追加句，并将 `万里路` 事件口径调整为玩家前往当前地点后必定触发。
- 在 `systems.md / development-freeze.md / numbers.md` 同步万里路固定顺序推进和必定触发口径，避免事件概率与固定经历系统互相矛盾。
