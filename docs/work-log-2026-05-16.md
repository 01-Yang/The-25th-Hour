# 2026-05-16 文档工作清单

## docs/events.md

- 新增 `触发限定`，明确 `课桌纸条` 每局保底、`幸运空位` 只在第 `5-6` 周触发、`微信通过` 归入交互事件池。
- 将 `微信通过` 从普通随机事件移到交互事件。
- 去掉重复的 `奶茶` 和 `老师让你重画` 条目。
- 同步事件池规模为：普通随机事件 `115`、交互事件 `44`、模型周事件 `12`、万里路事件 `12`，总计 `183`。
- 将事件结构化字段补充到 `repeatable / trigger_rules` 粒度，和系统规则口径统一。
- 新增普通随机事件 `好心人 / 暖心备注`，新增交互事件 `羽毛球 / 一起跑步吗 / 骑行的乐趣`，并同步事件池规模到普通随机事件 `117`、交互事件 `47`，总计 `188`。

## docs/numbers.md

- 补齐新增事件和交互事件的属性结算，保证 `events.md` 中每条事件都有对应数值。
- 增加高危状态行动限制与提示文案。
- 新增“行动联动修正”，明确 `软件技术` 影响 `正常速度画图 / 爆肝通宵赶图` 的进度效率与代价，`设计水平 + 创意审美` 共同影响 `方案推敲 / 场地调研` 的质量收益。
- 新增“每周自动恢复”，固定为周结算后 `精力 +12`、`压力 -10`。
- 将模型周材料调整为 `手工 / 激光切割 / 3D打印`，并补充简介、价格、收益和风险。
- 重写竞赛系统，补入 `霍普杯国际大学生建筑设计竞赛 / 台达杯国际太阳能建筑设计竞赛 / 台湾国际学生创意设计大赛 / 东南·中国建筑新人赛` 四项赛事的门槛、投稿分公式和奖项区间。
- 同步你手改的商店商品内容与数量，只调整了商品数值口径、限购次数、生效范围和规则说明，没有改商品文本内容本身，也没有增删商品。
- 重写保研、考研、留学、体制内、建筑工作、转行路线门槛，统一为大四上先锁定方向，再按对应条件读取结局。
- 补充毕业设计完成门槛与状态收尾条件。
- 修正答题规则为单题不显示对错、不显示正确答案。
- 用你最新提供的商店清单完整替换旧商店商品表；保留商品内容和数量，只重做了数值、限购、分类口径和生效说明。
- 将商店系统与行动系统重新对齐：明确 `星巴克会员周卡` 为唯一可使下一周行动数从 `3` 变为 `4` 的例外，且不叠加。
- 将商店内所有百分比、小数和阈值修正统一改为“四舍五入”口径。
- 将 `设计能力` 统一收口为 `设计水平`，并把“画图速度提高百分之十”明确绑定到 `正常速度画图 / 爆肝通宵赶图`。
- 将保研、考研、考公、考编、选调的考试规则改成“10题隐藏答题 + 内部折算现实分数线”，以便兼容 `390+` 这类真实分数表达。
- 将 `基层服务` 在数值规则里补成可触发的低档成功结局，不再和 `继续备考` 冲突。
- 将 AI 经历来源中的旧商品 `高性能笔记本分期` 同步替换为 `外星人笔记本电脑`。
- 将旧的极简程序事件格式升级为统一字段口径，并保留“首版最小必填字段”说明，避免规则层和程序层分叉。
- 补充竞赛结算闭环：明确单作品唯一投稿、按作品总分判奖、获奖给奖金、赛事与奖项写入简历。
- 将竞赛改为“不限时间投稿，只要作品满足条件即可投”；同时补充竞赛系统内可随时查看投稿记录与获奖情况。
- 补齐 `好心人 / 暖心备注 / 羽毛球 / 一起跑步吗 / 骑行的乐趣` 五条事件的属性结算，保证 `events.md` 与 `numbers.md` 一一对应。

## docs/question-banks.md

- 更新答题系统通用规则：作答后直接进入下一题，只在结束后显示总结果。
- 升学专业题库与行测题库都明确为：题库总量 `30`，单次抽题固定 `6` 易 `4` 难。
- 修正已有题目标注，使题库分层和你最终确认的 `6` 易 `4` 难规则一致。
- 增补升学专业题与行测题的内部折算分规则，和 `numbers.md` / `systems.md` 保持一致。

## docs/systems.md

- 增加高危状态行动限制。
- 增加每周自动恢复流程说明。
- 增加课题行动联动原则，明确“快”和“好”分开结算。
- 将后期路线改为大四上第 `1` 周强制锁定，未锁定不能继续行动。
- 明确每局只能选择 `保研 / 考研 / 留学 / 选调 / 考公 / 考编 / 建筑工作 / 转行` 之一。
- 明确转行必须同步选择具体岗位。
- 明确考研、体制内、建筑工作、转行失败时不跨路线读取成功结局。
- 明确保研、考研、留学在锁定路线时同步锁定对应档位或院校。
- 补充竞赛系统的单课题唯一投稿、结果即时公开和四项固定赛事。
- 增加路线考试、毕业设计门槛和 `没毕业` 读取规则。
- 补充结果揭示时机：评图等级在评图后当场公开，升学/体制内/建筑工作/转行的成败统一延后到最终结局结算揭示。
- 补充 `星巴克会员周卡` 作为每周固定 `3` 次行动的唯一例外，避免和固定行动数规则冲突。
- 将路线考试口径改为“10题隐藏答题 -> 内部折算现实分数线”，确保和考研 `390+`、体制内现实分数线叙述一致。

## docs/endings.md

- 保留原有结局故事文本为主，只调整触发条件和系统规则口径。
- 将升学、留学、体制内、建筑工作、转行结局统一改为“锁定方向 + 完成毕业设计 + 对应门槛”。
- 新增 `梦校直升` 与 `名校保研`，补齐此前缺失的两档保研成功结局。
- 新增 `没毕业` 结局，用于毕业设计未完成时的强制读取。
- 将 `生活费断裂` 内部状态对接到人生结局 `你破产了`。
- 修正成就触发条件，统一为大四上路线锁定和对应申请、报名、投递动作。
- 将 `留校读研` 强制限定为“大四上锁定保研且锁定档位为 `本校 / 211`”。
- 将考研、选调、考公、考编相关结局的考试条件统一改写为“答对至少几题（对应多少分）”。
- 本轮按你的要求重新核对了 `endings.md`，不再改动你手写的结局文本和分数；后续规则层全部向它对齐。
- 你确认接受的 10 个核心结论已正式回写到文档体系里，包括：`大学毕业` 成就必须完成毕业设计、`基层服务` 优先于 `继续备考`、`星巴克会员周卡` 为唯一额外行动例外、`心理疏导1小时` 将本学期崩溃阈值提高到 `>95` 连续 `2` 周等。
- 重新同步你最新手改的 `endings.md`：将升学、体制内相关结局的触发条件从旧的 `90/80/70/60` 口径统一到当前“隐藏答题 + 现实折算分”口径，但未改动你手写的结局正文与分数。
- 再次根据你最新手动修改后的结局系统做对齐：确认 `大学毕业` 成就改为“完整读完五年、未失败且完成毕业设计”，并确保 `endings.md / numbers.md / systems.md` 三份文档的考试、基层服务、继续备考口径一致。

## docs/content-plan.md

- 新增后期路线锁定规则摘要。
- 新增保研三档、考研三档、留学院校四档内容池。
- 明确题库内容维护在 `question-banks.md`，具体数值门槛维护在 `numbers.md`。

## docs/PRD.md

- 将后期路线从“大五统一判定”改为“大四上强制锁定，大五只做已锁定方向结果判定”。
- 将成功通关条件改为完整读完五年、未中途失败且完成毕业设计。

## docs/unresolved-issues.md

- 更新当前未解决问题为实现落地和实测调参事项。
- 移除旧的“没有未解决问题”口径，保留后续需要模拟和实现验证的风险点。
- 补充记录：保研三档现已全部补齐对应成功结局，分别对接 `梦校直升 / 名校保研 / 留校读研`。
- 补充开发前系统收敛冻结口径引用，明确后续讨论默认不回滚主系统边界。

## docs/development-freeze.md

- 新增开发前系统收敛冻结口径，记录两轮核心拷问后确认采用的架构边界。
- 将主循环、资源结算、高危失败、培养系统、商店竞赛实习、随机事件、AI 语境、后期路线和开发前验证统一写成冻结约束。
- 明确首版不再新增主系统，后续只做实现落地、内容补全、数值调参和体验打磨。

## docs/README.md

- 将 `development-freeze.md` 加入核心文档索引和文档分工。
- 补充开发前判断新增主系统时优先查看冻结口径的使用原则。

## docs/adr

- 新增 `0002-event-schema-and-competition-result-granularity.md`，记录事件字段颗粒度统一和竞赛结果落库口径。
- 更新 `0002`，补充竞赛“不限时间投稿”和“可查看获奖情况”的决策。

## CONTEXT.md

- 修正“作品集”定义，明确竞赛不直接增加作品集数值，只影响履历与后续路线读取。
